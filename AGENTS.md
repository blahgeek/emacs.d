# Agent Notes

## Package Documentation

When working with Emacs packages in this configuration, package documentation can often be found in:

```
straight/repos/<package-name>/README.md
```

For example, hydra documentation is at `straight/repos/hydra/README.md`.

This is useful for understanding:
- Package-specific syntax and macros
- Configuration options
- API documentation
- Usage examples
