# -*- mode: snippet -*-
# name: Source Bootstrap
# key: source_bootstrap
# uuid: cc_source_bootstrap
# --
`(my/snippet-copyright-as-comment)
`#include "`(let* ((root (or (projectile-project-root) default-directory))
       (rel (file-relative-name buffer-file-name root)))
  (file-name-with-extension rel ".h"))`"

